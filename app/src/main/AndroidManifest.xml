<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!--
        Only ever used to scan a relay's pairing QR, and requested at the moment Scan is tapped.
        Declared `required="false"` so a device without a camera can still install: typing the
        pairing code is a complete alternative, not a degraded one.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!--
        Shared-storage access for the Harness sandbox.

        The sandbox draws its boundary at firmware partitions: those stay read-only and unreachable
        under the untrusted-app SELinux domain, no matter what the approval policy decides. Everything
        the user owns lives in shared storage, so the file tools need scoped access to it.

        Media collections are read through the READ_MEDIA_* grants. No MANAGE_EXTERNAL_STORAGE is
        requested: that permission is reserved for file managers and would widen the grant to the
        whole volume, whereas the sandbox only needs the user-visible collections.
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <application
        android:name=".DshApplication"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:intentMatchingFlags="enforceIntentFilter"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.DshMobile"
        tools:targetApi="36">

        <!--
            `configChanges` handles the locale itself rather than being torn down for it.

            Applying a language is a configuration change, and the default response is to destroy
            and rebuild the activity. Between the two there is no window at all — not a window with
            the wrong background, none — so the screen goes to whatever is behind it, which is
            black. Declaring these two means the framework delivers `onConfigurationChanged`
            instead; Compose re-reads its resources and the text simply changes, with the transcript
            and scroll position kept. `layoutDirection` is listed with `locale` on purpose: without
            it, switching to Arabic or Urdu would still tear the activity down.
        -->
        <activity
            android:name=".ui.screens.pair.PortraitCaptureActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/zxing_CaptureTheme" />

        <activity
            android:name=".MainActivity"
            android:configChanges="locale|layoutDirection"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.DshMobile.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="host"
                    android:pathPrefix="/"
                    android:scheme="dshmobile" />
            </intent-filter>
        </activity>

        <service
            android:name=".connection.ConnectionService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".local.LocalExecutionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-initiated on-device AI task execution" />
        </service>

        <service
            android:name=".automation.HarnessWebhookService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-enabled local Harness webhook endpoint" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
