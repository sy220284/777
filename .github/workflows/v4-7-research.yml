name: v4-7-research

on:
  push:
    branches: [v4-7-optimization]
    paths:
      - 'tools/v4_7_*.py'
      - 'models/**'
      - 'data/**'
      - 'requirements.txt'
      - '.github/workflows/v4-7-research.yml'
  workflow_dispatch:

jobs:
  research:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - name: 安装依赖
        run: python -m pip install -r requirements.txt
      - name: 编译检查
        run: python -m compileall -q tools models
      - name: 运行新增专家研究
        run: python tools/v4_7_expert_expansion.py
      - name: 保存研究结果
        uses: actions/upload-artifact@v4
        with:
          name: v4-7-expert-expansion-summary
          path: backtests/v4_7/expert_expansion_summary.json
          if-no-files-found: error
