name: v46-low-signal-outball-robustness50

on:
  push:
    branches:
      - v4-6-nonlinear-challenger
    paths:
      - "tools/v4_6_low_signal_outball_robustness50.py"
      - ".github/workflows/v46-low-signal-outball-robustness50.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-24.04
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: 安装依赖
        run: python -m pip install -r requirements.txt
      - name: 编译检查
        run: python -m compileall -q tools/v4_6_low_signal_outball_robustness50.py tools/v4_6_low_signal_outball.py tools/v4_6_outball_poollock.py tools/v4_6_outball_corrected.py tools/v4_6_outball_features.py models
      - name: 运行50%重排鲁棒性审计
        run: python tools/v4_6_low_signal_outball_robustness50.py
      - name: 上传结果
        uses: actions/upload-artifact@v4
        with:
          name: v46-low-signal-outball-robustness50
          path: backtests/v4_6/low_signal_outball_robustness50.json
          if-no-files-found: error
